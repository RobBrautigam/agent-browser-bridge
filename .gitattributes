# Normalize line endings: LF in the repository on every platform.
* text=auto eol=lf

# Windows shims must keep CRLF or cmd.exe misreads them.
*.cmd text eol=crlf
*.bat text eol=crlf

# Binary
*.png binary
